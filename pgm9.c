#include<stdio.h>
void main()
{
int arrn[5];
int k,c;
scanf("%d%d",&&arrn,k);
printf("enter the array elements",arrn);
printf("enter the count to be added",k);
if(k=2)
{
c=arrn[1][1]+arrn[1][2];
}
else
printf("not valid");
}
